# LeagueOps Course Data

This repository contains course data for LeagueOps.

## Setup

Clone the repository and configure the remote:

```bash
git remote add origin https://github.com/labountyl24/leagueops-course-data.git
git push -u origin main
```
